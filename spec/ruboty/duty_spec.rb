require "spec_helper"

describe Ruboty::Duty do
  it "has a version number" do
    expect(Ruboty::Duty::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
